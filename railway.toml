[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

# Optional: mount a Railway Volume at /data and uncomment below so the JSON
# state survives redeploys.
# [deploy.envs]
# DATA_DIR = "/data"
